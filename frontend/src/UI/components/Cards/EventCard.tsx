type EventCardProps = {
    name: string,
    society: string,
    description: string,
    status: "Ongoing" | "Upcoming",
    date: string,
    link: HTMLLinkElement
}

const EventCard = () => {
    return <div>

    </div>
}

export default EventCard